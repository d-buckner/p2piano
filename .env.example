# MongoDB Credentials
# Use strong, unique credentials for production
MONGO_USERNAME=mongouser
MONGO_PASSWORD=your_secure_mongo_password_here

# Cookie Secret for Session Security
# Must be at least 32 characters long in production
COOKIE_SECRET=your_secure_cookie_secret_at_least_32_characters_long

# Optional: Override default MongoDB database name
# MONGO_DATABASE=p2piano

# Production Configuration
# NODE_ENV=production