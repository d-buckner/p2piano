import { extendTheme } from "@chakra-ui/react"

export default extendTheme({
  fonts: {
    body: 'Ysabeau',
    heading: 'Mortina Two',
  },
  colors: {
    background: '#000',
    foreground: '#FFF',
  },
});
